# Top-level dependencies. See requirements.lock.txt for the exact pinned,
# reproducible set (generated via `pip freeze` inside .venv).
numpy
opencv-python-headless
pillow
mss
rapidocr-onnxruntime
customtkinter

# Windows-only: window enumeration / client-rect lookup for targeted capture.
pywin32; sys_platform == "win32"

# Windows-only: Windows Graphics Capture (occlusion-proof, captures DirectX
# game frames that PrintWindow can't). Requires opencv-python (full) on Windows.
windows-capture; sys_platform == "win32"
